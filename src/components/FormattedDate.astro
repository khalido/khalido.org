---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-AU", {
      year: "2-digit",
      month: "short",
      day: "numeric",
    })
  }
</time>
